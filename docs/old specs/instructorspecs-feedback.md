We recently added instructor role to the platform. Instructor can only see things for the courses and the batches in those courses that they are teaching. Here are some feedback points that got left out and need to be done:

**General Instructor Changes**

- In the header, a tag is shown for the role. Remove color change on hover interaction as tag is not clickable

**Course Studio - Below are tab wise changes for the course**

**General Details tab**

- Remove the separator after course language and the "save changes" button as the information here is read-only now and cannot be changed
- There will be a collaborator logo always. Put a placeholder logo image here. It could be in rectangular or square form of 64px height and width

**Students tab**

- When choosing Alex who has only 1 batch assigned, the batch column shows a dropdown which has unassigned and the batch options. But, when there is only 1 batch assigned, remove the batch column from the table and show the batch name as text just above the table and below the search bar
- Remove the "Add Single Student" button as instructor is not allowed to do it

**Submissions tab**

- When selecting any submission and going into its details, show the batch filter only if there are multiple batches for the instructor. In case of single batch, hide the filter and mention the batch name under batch. Currently, I can see a batch filter and 6 batches even for Alex who has only 1 batch assigned.
- Move the total submissions, submissions received and qualified students (in case of assessments) labels above the count rather than them being below the count. This change will show on both admin and instructor roles
- In the table remove the batch column when there is only 1 batch assigned to the instructor
- In case of assessments, remove the "Approve Re-Attempt" action as it is not available for the instructor
